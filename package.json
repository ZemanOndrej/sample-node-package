{
	"name": "adacta",
	"version": "1.0.0",
	"description": "simple typescript package",
	"main": "./src/index.ts",
	"scripts": {
		"test": "./node_modules/.bin/mocha --require ts-node/register --watch-extensions ts,tsx  ./test/*.spec.ts --reporter mochawesome --require mochawesome/register",
		"build": "tsc -p .",
		"run": " npm run cleanup && npm run build && node ./bin/index.js",
		"cleanup": "rimraf ./bin"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/mocha": "^8.2.1",
		"@types/node": "^14.14.31",
		"mocha": "^8.3.0",
		"mochawesome": "^6.2.2",
		"rimraf": "^3.0.2",
		"ts-node": "^9.1.1",
		"typescript": "^4.1.5"
	}
}
